<style>
    .lf { font-family: DejaVu Sans, Arial, Helvetica, sans-serif; color: #111; font-size: 11px; line-height: 1.35; }
    .lf table { width: 100%; border-collapse: collapse; }
    .lf-head td { border: 0; vertical-align: middle; padding: 0 0 6px; }
    .lf-logo { width: 72px; }
    .lf-logo img { width: 64px; height: auto; }
    .lf-company { text-align: center; }
    .lf-co { font-size: 13px; font-weight: 700; letter-spacing: 0.3px; text-transform: uppercase; }
    .lf-sub { font-size: 9px; color: #333; }
    .lf-meta { width: 140px; text-align: right; font-size: 9px; }
    .lf-grid { margin-bottom: 6px; }
    .lf-grid td { border: 1px solid #111; padding: 5px 6px; vertical-align: top; }
    .lf-grid .lbl { font-size: 9px; font-weight: 700; text-transform: uppercase; width: 18%; background: #f3f3f3; }
    .lf-grid .val { font-weight: 600; }
    .lf-banner {
        background: #111;
        color: #fff;
        font-size: 11px;
        font-weight: 700;
        letter-spacing: 1px;
        text-align: center;
        padding: 4px 6px;
        margin: 8px 0 0;
        border: 1px solid #111;
    }
    .lbl-inline { font-size: 9px; font-weight: 700; text-transform: uppercase; margin-bottom: 3px; }
    .days-line { margin-top: 8px; }
    .days-val { display: inline-block; min-width: 48px; border-bottom: 1px solid #111; text-align: center; font-weight: 700; padding: 0 8px; }
    .lf-checks td { border: 0; padding: 3px 2px; font-size: 10px; }
    .chk {
        display: inline-block;
        width: 12px;
        height: 12px;
        border: 1px solid #111;
        text-align: center;
        font-size: 9px;
        font-weight: 700;
        line-height: 12px;
        margin-right: 4px;
    }
    .special-note { font-size: 8px; color: #333; margin: 2px 0 0 18px; }
    .special-sel { font-size: 9px; font-weight: 700; margin-left: 18px; }
    .u { display: inline-block; min-width: 88px; border-bottom: 1px solid #111; padding: 0 4px; }
    .reason { min-height: 36px; margin-top: 4px; }
    .declare { font-size: 10px; margin: 8px 0; text-align: justify; }
    .declare.tight { margin: 4px 0 6px; }
    .sign-cell { height: 64px; }
    .print-name { font-weight: 700; margin-top: 4px; }
    .sig-img { height: 36px; max-width: 180px; display: block; margin: 2px 0; }
    .sig-img.sm { height: 28px; max-width: 140px; }
    .dec-cell { font-size: 10px; font-weight: 700; }
    .note { font-size: 8px; font-style: italic; margin: 10px 0; }
    .pay { margin-top: 10px; font-weight: 700; font-size: 10px; }
    .ent-title { font-weight: 700; font-size: 10px; margin-bottom: 4px; text-transform: uppercase; }
    .ent-grid { margin-top: 6px; }
    .ent-grid td { border: 0; padding: 2px 4px 2px 0; font-size: 9px; }
    .tiny { font-size: 8px; margin-top: 4px; }
    .lf-foot { margin-top: 8px; font-size: 9px; }
    .lf-foot td { border: 0; padding: 2px 0; }
    .lf-foot .right { text-align: right; }
    .top { vertical-align: top; }
</style>
